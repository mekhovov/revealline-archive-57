# RevealLine Archive57

This dedicated archive preserves the original v0.88.0 distribution from
`mekhovov/revealline`, release `394233790`, without rebuilding historical source.
Preparation is local only; this document does not assert Archive57 deployment or public acceptance.

## Frozen identity

- Source: `950f19045facacf5151c90661de1ca28d30658df`
- Source tree: `0c288a694a4c2cba04a7ae331eb3a376bdd4c811`
- Annotated tag: `df3e545f03432486a7eee321fb0799fb7490a1cb`
- Accepted consumer donor: Archive56 merge `592d0afe42f66dfb9fe95962c80adfaf236cd8ef`
- Expected public artifact: 1,088 files, 590,907,371 bytes.

`source-lock.json` pins the original metadata, distribution, qualification, exact-source
waiver policy, extractor and complete expected inventory. The original qualification
is `qualified-with-test-waiver`; automated suites are waived, not passed. GitHub reports
the release immutable flag as false, so original assets are preserved by policy and hashes.

The Actions preview extracts only the original distribution ZIP, verifies its manifest
and CRC, then independently rereads every expected byte. It retains the 800,000,000-byte
artifact budget and 3 GiB free-space guard. PR previews do not upload or deploy Pages.
Only a reviewed main merge can publish through the guarded production jobs.

All files under `authority/` and original metadata retain their source bytes, including
whitespace in the raw commit object. Such evidence is never reformatted as source code.
The local source-lock/inventory checks are not a hosted build or a public availability pass.

Before creation or publication, re-read authority, review this exact scaffold tree, use
an empty-main bootstrap and a separate feature PR, and obtain explicit bounded writer
handoff. Preserve every previous archive, historical route and original failed observation.
